{
  "name": "plugin-agent-autoresponse",
  "version": "0.0.0",
  "private": true,
  "description": "Enables your agents to send canned responses when chatting with a customer",
  "scripts": {
    "postinstall": "flex-plugin pre-script-check"
  },
  "dependencies": {
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "flex-plugin-scripts": "4.3.2-beta.0"
  },
  "devDependencies": {
    "@twilio/flex-ui": "^1"
  }
}
